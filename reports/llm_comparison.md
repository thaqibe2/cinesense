# NLP approach comparison: classical vs LLM (title -> genre)

> This file is auto-generated by [`src/llm_compare.py`](../src/llm_compare.py).
> Run it with an OpenAI key to populate the real numbers (small, capped sample,
> well under $0.01):
>
> ```bash
> export OPENAI_API_KEY=sk-...      # set OPENAI_API_KEY=... on Windows
> python src/llm_compare.py 60
> ```
>
> It compares the classical char-TFIDF + Logistic Regression title->genre model
> against an OpenAI LLM prompted with the title only, scoring both (micro/macro-F1)
> against the true genre flags on the same sampled test films, and overwrites this
> file with a results table.

_Results table appears here after the script is run._
